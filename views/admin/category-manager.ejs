<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Danh mục sản phẩm</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        body { background-color: #f0f0f1; }
        .wp-card { background: #fff; border: 1px solid #dcdcde; box-shadow: 0 1px 1px rgba(0,0,0,.04); padding: 20px; }
        .table-head-wp { background-color: #f0f0f1; text-transform: uppercase; font-size: 12px; font-weight: bold; color: #646970; }
    </style>
</head>
<body>

    <div class="d-flex">
        <%- include('partials/sidebar.ejs') %>

        <div class="flex-grow-1 p-4" style="height: 100vh; overflow-y: auto;">
            <h2 class="mb-4 fw-bold">Danh mục sản phẩm</h2>

            <div class="row">
                
                <div class="col-md-4">
                    <div class="wp-card mb-3">
                        <h5 class="fw-bold mb-3">Thêm danh mục mới</h5>
                        <form action="/admin/categories" method="POST">
                            
                            <div class="mb-3">
                                <label class="form-label fw-bold">Tên đường dẫn</label>
                                <input type="text" name="name" class="form-control" placeholder="Ví dụ: Áo Thun" required>
                                <div class="form-text small">Tên riêng sẽ hiện trên trang web của bạn.</div>
                            </div>

                            <div class="mb-3">
                                <label class="form-label fw-bold">Mô tả</label>
                                <textarea name="description" class="form-control" rows="4"></textarea>
                                <div class="form-text small">Mô tả này thường không được hiển thị trong các giao diện, tuy nhiên có vài giao diện có thể hiển thị nó.</div>
                            </div>

                            <button type="submit" class="btn btn-primary">Thêm danh mục</button>
                        </form>
                    </div>
                </div>

                <div class="col-md-8">
                    <div class="wp-card">
                        <table class="table table-hover align-middle">
                            <thead class="table-head-wp">
                                <tr>
                                    <th scope="col"><input type="checkbox" class="form-check-input"></th>
                                    <th scope="col">Tên</th>
                                    <th scope="col">Slug (Đường dẫn)</th>
                                    <th scope="col">Mô tả</th>
                                    <th scope="col" class="text-end">Lượt</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% if (categories.length > 0) { %>
                                    <% categories.forEach(cat => { %>
                                        <tr>
                                            <td><input type="checkbox" class="form-check-input"></td>
                                            <td>
                                                <strong><a href="/admin/edit-category/<%= cat._id %>" class="text-decoration-none text-dark fw-bold fs-6"><%= cat.name %></a></strong>
                                                
                                                <div class="small mt-1 opacity-75">
                                                    <a href="/admin/edit-category/<%= cat._id %>" class="text-primary text-decoration-none me-2">Sửa</a>
                                                    
                                                    <span class="text-muted">|</span>

                                                    <form action="/admin/delete-category" method="POST" class="d-inline" onsubmit="return confirm('Bạn có chắc chắn muốn xóa danh mục này?');">
                                                        <input type="hidden" name="categoryId" value="<%= cat._id %>">
                                                        <button type="submit" class="btn btn-link p-0 text-danger text-decoration-none ms-2" style="font-size: 13px;">Xóa vĩnh viễn</button>
                                                    </form>
                                                </div>
                                            </td>
                                            <td><%= cat.slug %></td>
                                            <td class="small text-muted"><%= cat.description ? cat.description : '—' %></td>
                                            <td class="text-end"><span class="badge bg-secondary rounded-pill">0</span></td>
                                        </tr>
                                    <% }) %>
                                <% } else { %>
                                    <tr>
                                        <td colspan="5" class="text-center py-4">Chưa có danh mục nào.</td>
                                    </tr>
                                <% } %>
                            </tbody>
                        </table>
                    </div>
                </div>

            </div> </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>