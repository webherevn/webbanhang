<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= pageTitle %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .product-card img {
            height: 300px; /* Cố định chiều cao ảnh cho đều */
            object-fit: cover; /* Cắt ảnh cho vừa khung */
        }
        .product-card:hover {
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
            transition: 0.3s;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
        <div class="container">
            <a class="navbar-brand" href="/">FASHION SHOP</a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/cart">Giỏ hàng (0)</a>
                <a class="nav-link" href="/admin/add-product">Admin</a>
            </div>
        </div>
    </nav>

    <div class="container">
        <h2 class="text-center mb-4">Sản Phẩm Mới Nhất</h2>
        
        <div class="row">
            <% if (products.length > 0) { %>
                <% products.forEach(product => { %>
                    <div class="col-md-3 col-sm-6 mb-4">
                        <div class="card product-card h-100">
                            <img src="<%= product.images[0] %>" class="card-img-top" alt="<%= product.name %>">
                            <div class="card-body d-flex flex-column">
                                <h5 class="card-title"><%= product.name %></h5>
                                <p class="card-text text-danger fw-bold">
                                    <%= product.basePrice.toLocaleString('vi-VN') %> đ
                                </p>
                                <a href="/products/<%= product.slug %>" class="btn btn-outline-dark mt-auto">Xem chi tiết</a>
                            </div>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="col-12 text-center">
                    <p>Chưa có sản phẩm nào. Hãy vào Admin thêm nhé!</p>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>