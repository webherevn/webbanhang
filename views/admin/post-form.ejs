<%- include('../partials/head.ejs') %>
<script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
<link href="https://cdn.jsdelivr.net/npm/summernote@0.8.18/dist/summernote-lite.min.css" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/summernote@0.8.18/dist/summernote-lite.min.js"></script>

<style>
    body { background-color: #f0f0f1; margin: 0; }
    .admin-wrapper { display: flex; min-height: 100vh; }
    .admin-sidebar-container { width: 240px; position: fixed; top: 0; left: 0; bottom: 0; background-color: #23282d; z-index: 1000; }
    .admin-content { flex: 1; margin-left: 240px; padding: 30px; }
    .wp-card { background: #fff; border: 1px solid #c3c4c7; box-shadow: 0 1px 1px rgba(0,0,0,.04); padding: 20px; margin-bottom: 20px; }
    .note-editor { border: 1px solid #c3c4c7 !important; }
</style>
</head>
<body>
    <div class="admin-wrapper">
        <div class="admin-sidebar-container"><%- include('./partials/sidebar.ejs') %></div>

        <div class="admin-content">
            <h2 class="fw-bold h4 mb-4"><%= editing ? 'Chỉnh sửa bài viết' : 'Viết bài mới' %></h2>
            
            <form action="/admin/<%= editing ? 'edit-post' : 'add-post' %>" method="POST" enctype="multipart/form-data">
                <% if (editing) { %>
                    <input type="hidden" name="postId" value="<%= post._id %>">
                <% } %>

                <div class="row">
                    <div class="col-md-9">
                        <div class="mb-4">
                            <input type="text" name="title" class="form-control form-control-lg fw-bold" 
                                   value="<%= editing ? post.title : '' %>" placeholder="Nhập tiêu đề bài viết..." required shadow-sm>
                        </div>

                        <div class="wp-card">
                            <label class="fw-bold small mb-2 text-uppercase">Nội dung chi tiết</label>
                            <textarea name="content" id="content-editor"><%= editing ? post.content : '' %></textarea>
                        </div>

                        <div class="wp-card">
                            <label class="fw-bold small mb-2 text-uppercase">Tóm tắt ngắn (Summary)</label>
                            <textarea name="summary" class="form-control" rows="3" placeholder="Viết mô tả ngắn để hiển thị ngoài danh sách..."><%= editing ? post.summary : '' %></textarea>
                        </div>
                    </div>

                    <div class="col-md-3">
                        <div class="wp-card">
                            <h6 class="fw-bold small mb-3 text-uppercase border-bottom pb-2">Đăng bài viết</h6>
                            <button type="submit" class="btn btn-primary w-100 fw-bold shadow-sm">
                                <%= editing ? 'Cập nhật bài viết' : 'Đăng bài viết' %>
                            </button>
                            <a href="/admin/posts" class="btn btn-link btn-sm w-100 mt-2 text-muted">Hủy bỏ</a>
                        </div>

                        <div class="wp-card">
                            <h6 class="fw-bold small mb-3 text-uppercase border-bottom pb-2">Chuyên mục</h6>
                            <select name="categoryId" class="form-select form-select-sm" required>
                                <% categories.forEach(cat => { %>
                                    <option value="<%= cat._id %>" 
                                        <%= (editing && post.category && post.category._id.toString() === cat._id.toString()) ? 'selected' : '' %>>
                                        <%= cat.name %>
                                    </option>
                                <% }) %>
                            </select>
                        </div>

                        <div class="wp-card">
                            <h6 class="fw-bold small mb-3 text-uppercase border-bottom pb-2">Ảnh bài viết</h6>
                            <% if (editing && post.thumbnail) { %>
                                <img src="<%= post.thumbnail %>" class="img-fluid mb-2 border rounded shadow-sm">
                            <% } %>
                            <input type="file" name="thumbnail" class="form-control form-control-sm">
                        </div>
                        <div class="wp-card shadow-sm border-top border-4 border-primary">
                            <h6 class="fw-bold small mb-3 text-uppercase"><i class="fas fa-search me-2"></i>Phân tích SEO</h6>
                            
                            <div class="mb-3">
                                <label class="small fw-bold">Từ khóa chính (Focus Keyword)</label>
                                <input type="text" id="focusKeyword" name="focusKeyword" class="form-control form-control-sm" 
                                    value="<%= editing ? post.focusKeyword : '' %>" placeholder="Ví dụ: thời trang nam">
                            </div>

                            <ul class="list-unstyled small mb-0" id="seo-checklist">
                                <li class="mb-2 seo-item" data-check="title-keyword">
                                    <i class="fas fa-circle-notch text-muted me-2"></i>Từ khóa trong tiêu đề
                                </li>
                                <li class="mb-2 seo-item" data-check="content-length">
                                    <i class="fas fa-circle-notch text-muted me-2"></i>Độ dài nội dung (>300 từ)
                                </li>
                                <li class="mb-2 seo-item" data-check="desc-keyword">
                                    <i class="fas fa-circle-notch text-muted me-2"></i>Từ khóa trong mô tả ngắn
                                </li>
                                <li class="mb-2 seo-item" data-check="slug-keyword">
                                    <i class="fas fa-circle-notch text-muted me-2"></i>Từ khóa trong đường dẫn
                                </li>
                                <li class="mb-0 seo-item" data-check="img-alt">
                                    <i class="fas fa-circle-notch text-muted me-2"></i>Ảnh có thẻ Alt từ khóa
                                </li>
                            </ul>

                            <div class="mt-3 pt-3 border-top">
                                <div class="d-flex justify-content-between align-items-center">
                                    <span class="small fw-bold">Điểm SEO:</span>
                                    <span id="seo-score-text" class="badge bg-secondary">0/100</span>
                                </div>
                                <div class="progress mt-2" style="height: 6px;">
                                    <div id="seo-progress-bar" class="progress-bar bg-danger" style="width: 0%"></div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </form>
        </div>
    </div>

    <script>
        $(document).ready(function() {
            $('#content-editor').summernote({
                height: 500,
                placeholder: 'Bắt đầu viết nội dung của bạn...',
                toolbar: [
                    ['style', ['style']],
                    ['font', ['bold', 'underline', 'clear', 'italic']],
                    ['color', ['color']],
                    ['para', ['ul', 'ol', 'paragraph']],
                    ['table', ['table']],
                    ['insert', ['link', 'picture', 'video', 'hr']],
                    ['view', ['fullscreen', 'codeview']]
                ]
            });
        });
    </script>
    <script>
        $(document).ready(function() {
            $('#page-editor').summernote({ height: 500 });
        });
    </script>

    <script src="/js/admin-seo.js"></script>
</body>
</html>